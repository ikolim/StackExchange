#:kivy 1.10.0

<RootWidget>:
    orientation: "vertical"
    Image:
        source: root.os["kivy"]
    Image:
        source: root.os["linux"]
    Image:
        source: root.os["mac"]
    Image:
        source: root.os["windows"]
    Image:
        source: root.os["raspberrypi"]
